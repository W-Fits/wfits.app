{
  "name": "shared-utils",
  "version": "0.1.0",
  "scripts": {
    "generate:requirements": "poetry export --without-hashes --format=requirements-prod.txt > requirements-prod.txt",
    "test": "poetry run pytest"
  }
}