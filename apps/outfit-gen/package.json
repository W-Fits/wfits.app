{
  "name": "outfit-gen-api",
  "version": "0.1.0",
  "scripts": {
    "dev": "poetry run python run.py",
    "generate:requirements": "poetry export --without-hashes --format=requirements-prod.txt > requirements-prod.txt",
    "test": "cross-env HOME=%USERPROFILE% poetry run pytest"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}